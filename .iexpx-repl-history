.exit
const  x, y: [x, y] } = { x, y } ;
const { x, y: [x, y] } = { x, y } ;
const { x, y } = { x, y } ;
const x = 5 ;
.exit
const x = 5 ;
.exit
const x = 5 ;
.exit
const { x, y } = { x, y } ;
const x = 5 ;
const { x, y } = { x, y } ;
const x = 5 ;
const { x, y } = { x, y } ;
.exit
const { x, y } = { x, y } ;
.exit
const { x, y } = { x, y } ;
.exit
const { x, y } = { x, y } ;
const x = 5 ;
.exit
const x = 5 ;
const { x, y } = { x, y } ;
.exit
const { x, y } = { x, y } ;
.exit
const { x, y } = { x, y } ;
.exit x = 5 ;